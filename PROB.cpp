#include <bits/stdc++.h>

using namespace std;

int main() {

	ios::sync_with_stdio(false);
	cin.tie(NULL);

	int t;
	long a, b, c, d;
	cin>>t;
	while(t--) {
		cin>>a>>b>>c>>d;
		cout<<(((double) a) / (double)(a+b)) <<endl; 
	}
  return 0;
}
